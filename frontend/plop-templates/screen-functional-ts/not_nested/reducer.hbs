import { combineReducers } from 'redux';
import { RootState } from '@root/store';
import { reducerCreator } from '@helpers/reducer.helper';
import { {{camelCase name}}Reducer } from '@screens/{{pascalCase name}}/containers/{{pascalCase name}}Page/reducer';
/* PlopJS import placeholder. Do not remove */

const requests = combineReducers({
  /* PlopJS request placeholder. Do not remove */
});

export default combineReducers({
  requests,
  data: {{camelCase name}}Reducer
});

const reqs = (state: RootState) => state.{{camelCase name}}.requests;
const data = (state: RootState) => state.{{camelCase name}}.data;

/* PlopJS request_extractor placeholder. Do not remove */
