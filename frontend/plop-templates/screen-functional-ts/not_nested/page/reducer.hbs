import { createReducer, PayloadAction } from '@reduxjs/toolkit';

export interface I{{pascalCase name}}ReducerState {
}

const initialState: I{{pascalCase name}}ReducerState = {
};

export const {{camelCase name}}Reducer = createReducer(initialState, {

});
