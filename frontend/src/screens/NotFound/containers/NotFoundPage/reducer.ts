import { createReducer, PayloadAction } from '@reduxjs/toolkit';

export interface IPageNotFoundReducerState {
}

const initialState: IPageNotFoundReducerState = {
};

export const pageNotFoundReducer = createReducer(initialState, {

});
