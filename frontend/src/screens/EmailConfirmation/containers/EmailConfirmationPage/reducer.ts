import { createReducer, PayloadAction } from '@reduxjs/toolkit';

export interface IEmailConfirmationReducerState {
}

const initialState: IEmailConfirmationReducerState = {
};

export const emailConfirmationReducer = createReducer(initialState, {

});
